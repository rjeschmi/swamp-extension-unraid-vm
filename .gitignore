# Swamp runtime data (not needed for reconstruction)
.swamp/data/
.swamp/outputs/
.swamp/workflow-runs/
.swamp/workflows-evaluated/
.swamp/telemetry/

# Encryption keyfiles (NEVER commit - allows decrypting secrets)
.swamp/secrets/

# Claude Code configuration - ignore most, but track hooks and settings
.claude/*
!.claude/hooks/
!.claude/settings.json

# Vault data
vaults/

# Workflow run history in logical views
workflows/*/runs/

# Model runtime data symlinks in logical views
models/*/outputs/
models/*/source/
models/*/step/
models/*/type/
models/*/modelName/
models/*/specName/
models/*/workflow/

# OpenStack credentials
openrc.sh
