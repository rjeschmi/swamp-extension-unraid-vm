manifestVersion: 1
name: "@rjeschmi/k3s"
version: "2026.02.27.2"
description: "Install and manage k3s on VMs over SSH. Discovers VM IPs via virsh guest agent and waits for the node to reach ready state."
models:
  - k3s.ts
